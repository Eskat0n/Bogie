 log4r_config:
   loggers:
   - name: default
     level: DEBUG
     outputters:
     - console
   outputters:
   - name: console
     type: StdoutOutputter
     default: true
     formatter:
       type: PatternFormatter
       pattern: '%d [%l] - %m'
       date_pattern: '%H:%M:%S'